<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wx_hospital.mapper.SecSecondDepartmentMapper" >
    <select id="SelectFirDep" resultType="com.wx_hospital.pojo.SecSecondDepartment">
        SELECT
         de.*
        FROM
         sec_first_department fi,sec_second_department de
        WHERE
         fi.hospital_id=de.first_department_id and fi.hospital_id=#{hospitalId}
    </select>
</mapper>